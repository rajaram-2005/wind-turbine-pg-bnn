apiVersion: v1
kind: Secret
metadata:
  name: aerovigil-secrets
  namespace: aerovigil
type: Opaque
stringData:
  # Base64 encode your HF token: echo -n 'hf_xxx' | base64
  HF_TOKEN: "<base64-encoded-huggingface-token>"
  # Optional: API key for external auth
  API_KEY: "<base64-encoded-api-key>"
